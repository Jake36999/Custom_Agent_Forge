# ─── CRITICAL COMPATIBILITY LOCKS ──────────────────────────────
peft==0.18.1  # Prevent FP8 dtype crash with torch 2.4
# ─── CRITICAL COMPATIBILITY LOCKS ──────────────────────────────
peft==0.18.1  # Prevent FP8 dtype crash with torch 2.4
# ─── Core Runtime ───────────────────────────────────────────────
tqdm>=4.0
pyyaml>=6.0
numpy<2.0.0  # ML stack and matplotlib compatibility
networkx>=3.2
pandas<3  # for streamlit compatibility
pytz
pydantic>=2.6
typing-extensions>=4.9
jsonlines>=4.0

# ─── Validation & Formatting ───────────────────────────────────
tiktoken>=0.7
black>=24.0
sqlglot>=23.0

# ─── NLP / Semantic Processing ─────────────────────────────────
spacy>=3.7
amrlib>=0.8
penman>=1.2

# ─── Distributed Execution ─────────────────────────────────────
ray>=2.9

# ─── Task Orchestration (Celery + Redis) ───────────────────────
celery[redis]>=5.4
redis>=5.0

# ─── API Layer ─────────────────────────────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27

# ─── Monitoring (optional) ────────────────────────────────────
flower>=2.0

# ─── Polyglot AST Slicing ─────────────────────────────────────
tree-sitter>=0.20
tree-sitter-python
tree-sitter-cpp
tree-sitter-rust
tree-sitter-java
tree-sitter-ruby
tree-sitter-c-sharp

# ─── Test ──────────────────────────────────────────────────────
pytest>=8.0
